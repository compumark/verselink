const materials = ['Agricium','Aluminum','Aphorite','Aslarite','Beryl','Bexalite','Boron','Borase','Copper','Corundum','Diamond','Dolivine','Gold','Hadanite','Hephaestanite','Iron','Janalite','Laranite','Ouratite','Quantainium','Savrilium','Stileron','Taranite','Titanium','Tungsten'];
const codes = {Agricium:'AGRI',Aluminum:'ALUM',Aphorite:'APHO',Aslarite:'ASLA',Beryl:'BERY',Bexalite:'BEXA',Boron:'BORO',Borase:'BORA',Copper:'COPP',Corundum:'CORU',Diamond:'DIAM',Dolivine:'DOLI',Gold:'GOLD',Hadanite:'HADA',Hephaestanite:'HEPH',Iron:'IRON',Janalite:'JANA',Laranite:'LARA',Ouratite:'OURA',Quantainium:'QUAN',Savrilium:'SAVR',Stileron:'STIL',Taranite:'TARA',Titanium:'TITA',Tungsten:'TUNG'};
const qualityRanges = {1:[1,399],2:[400,599],3:[600,699],4:[700,799],5:[800,899],6:[900,949],7:[950,998],8:[999,1000]};
const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const root = () => document.querySelector('#content');
const state = { groupId: '', groups: [], materials: [], error: '', mineOnly: false };
const request = async (path, options = {}) => { const response = await fetch(path, options); const body = await response.json(); if (!response.ok) throw new Error(body.error || 'Material inventory request failed'); return body; };
const style = `<style id="material-inventory-css">
.mi-app{padding:24px;color:var(--text)}.mi-app h1{color:var(--bright);letter-spacing:.1em}.mi-toolbar{display:flex;gap:12px;align-items:end;flex-wrap:wrap;margin:16px 0;padding:16px;border:1px solid var(--border);background:var(--panel)}.mi-toolbar label,.mi-form label{display:grid;gap:6px;color:var(--muted);font-size:12px;letter-spacing:.08em}.mi-app select,.mi-app input,.mi-app textarea,.mi-app button,.mi-overlay select,.mi-overlay input,.mi-overlay textarea,.mi-overlay button{font:inherit;color:var(--text);background:var(--panel2);border:1px solid var(--border);padding:10px}.mi-app button,.mi-overlay button{cursor:pointer;text-transform:uppercase;letter-spacing:.04em;font-weight:600}.mi-app button.active{border-color:var(--accent);background:var(--accent);color:var(--bg);box-shadow:0 0 16px var(--glow)}.mi-app button:hover,.mi-overlay button:hover{border-color:var(--accent);box-shadow:0 0 12px var(--glow)}.mi-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(165px,1fr));gap:14px}.mi-card{min-height:165px;padding:14px;border:1px solid var(--border);background:linear-gradient(145deg,var(--panel),var(--panel2));text-align:left}.mi-card:not(.empty):hover{border-color:var(--accent);box-shadow:0 0 20px var(--glow)}.mi-card.empty{opacity:.45}.mi-code{font-size:22px;font-weight:700;color:var(--accent)}.mi-name{margin:15px 0 8px;color:var(--bright);font-weight:700}.mi-scu{font-size:20px}.mi-meta{margin-top:8px;color:var(--muted);font-size:11px}.mi-systems{margin-top:8px;color:var(--muted);font-size:11px;letter-spacing:.04em}.mi-overlay{position:fixed;inset:0;z-index:100;display:grid;place-items:center;padding:20px;background:#000b}.mi-panel{width:min(1080px,100%);max-height:calc(100vh - 40px);overflow:auto;padding:28px;border:1px solid var(--accent);background:linear-gradient(145deg,var(--panel),var(--panel2));box-shadow:0 0 28px var(--glow)}.mi-head{display:flex;justify-content:space-between;align-items:start;gap:18px;padding-bottom:18px;border-bottom:1px solid var(--border)}.mi-head h2{margin:4px 0 0;color:var(--bright);font-size:28px;letter-spacing:.02em}.mi-head>button,.mi-table button{color:var(--bright);border-color:var(--accent);background:transparent}.mi-group-target{margin-top:7px;color:var(--accent);font-size:12px;letter-spacing:.08em}.mi-summary{display:flex;gap:28px;margin:22px 0}.mi-summary div{display:grid;gap:4px}.mi-summary strong{font-size:18px;color:var(--bright)}.mi-table{width:100%;border-collapse:collapse}.mi-table th,.mi-table td{padding:12px 10px;border-bottom:1px solid var(--border);text-align:left}.mi-table th{color:var(--accent);font-size:11px;letter-spacing:.08em}.mi-table td{color:var(--text)}.mi-table tbody tr:hover{background:color-mix(in srgb,var(--accent) 7%,transparent)}.mi-note{color:var(--accent);cursor:help;font-size:17px}.mi-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:24px}.mi-form .wide{grid-column:1/-1}.mi-form input:focus,.mi-form select:focus,.mi-form textarea:focus{outline:1px solid var(--accent);border-color:var(--accent);box-shadow:0 0 12px var(--glow)}.mi-actions{display:flex;gap:10px;margin-top:22px}.mi-actions button:last-child{background:var(--accent);color:var(--bg);font-weight:700}.mi-error{color:#ff9f9f}@media(max-width:700px){.mi-app{padding:14px}.mi-panel{padding:18px}.mi-form{grid-template-columns:1fr}.mi-table{display:block;overflow:auto;white-space:nowrap}}</style>`;

const logoCss = '<style>.mi-brand{display:flex;align-items:center;gap:16px;margin-bottom:20px}.mi-logo{display:block;flex:0 0 88px;width:88px;height:88px;object-fit:contain;filter:drop-shadow(0 0 12px var(--glow))}.mi-brand-copy h1{margin:5px 0 8px}@media(max-width:700px){.mi-brand{gap:12px}.mi-logo{flex-basis:64px;width:64px}}</style>';
const qualityInfoCss = '<style>.mi-group-control{position:relative;display:flex;align-items:end;gap:8px}.mi-quality-info{display:grid;place-items:center;width:34px;height:34px;padding:0!important;border-radius:50%;color:var(--accent)!important;font-size:18px;line-height:1}.mi-quality-tooltip{position:absolute;top:calc(100% + 10px);left:0;z-index:10;display:grid;grid-template-columns:repeat(2,max-content);gap:5px 16px;min-width:210px;padding:12px 14px;border:1px solid var(--accent);background:var(--panel2);box-shadow:0 0 18px var(--glow);opacity:0;visibility:hidden;pointer-events:none;transition:opacity .15s ease,visibility .15s ease}.mi-quality-tooltip strong{grid-column:1/-1;color:var(--accent);font-size:11px;letter-spacing:.08em}.mi-quality-tooltip span{font-size:12px}.mi-quality-tooltip span:nth-child(even){color:var(--bright);font-weight:700}.mi-quality-info:hover + .mi-quality-tooltip,.mi-quality-info:focus + .mi-quality-tooltip{opacity:1;visibility:visible}@media(max-width:700px){.mi-quality-tooltip{left:auto;right:0}}.mi-owner-only{color:var(--muted);font-size:11px;letter-spacing:.06em}</style>';
const withdrawCss = '<style>.mi-withdraw-panel{width:min(520px,100%)}.mi-withdraw-details{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin:22px 0;padding:14px 0;border-top:1px solid var(--border);border-bottom:1px solid var(--border)}.mi-withdraw-details span{display:block;color:var(--muted);font-size:11px;letter-spacing:.08em}.mi-withdraw-details strong{display:block;margin-top:5px;color:var(--bright)}.mi-withdraw-form{display:grid;gap:12px}.mi-withdraw-form label{display:grid;gap:6px;color:var(--muted);font-size:12px;letter-spacing:.08em}.mi-withdraw-form input{font-size:18px}.mi-withdraw-error{min-height:18px;color:#ffb7ad;font-size:12px}@media(max-width:520px){.mi-withdraw-details{grid-template-columns:1fr}}</style>';
const moveCss = '<style>.mi-move-panel{width:min(560px,100%)}.mi-move-form{display:grid;gap:14px;margin-top:22px}.mi-move-form label{display:grid;gap:6px;color:var(--muted);font-size:12px;letter-spacing:.08em}.mi-location-results{max-height:220px;overflow:auto;display:grid;gap:6px}.mi-location-results button{text-align:left;text-transform:none;font-weight:400;width:100%}.mi-location-results button.selected{border-color:var(--accent);background:color-mix(in srgb,var(--accent) 14%,transparent)}.mi-move-error{min-height:18px;color:#ffb7ad;font-size:12px}</style>';
const moveAllCss = '<style>.mi-move-all-panel{width:min(560px,100%)}.mi-move-all-form{display:grid;gap:14px;margin-top:22px}.mi-move-all-form label{display:grid;gap:6px;color:var(--muted);font-size:12px;letter-spacing:.08em}.mi-move-all-info{color:var(--muted);line-height:1.5}.mi-move-all-results{max-height:220px;overflow:auto;display:grid;gap:6px}.mi-move-all-results button{text-align:left;text-transform:none;font-weight:400;width:100%}.mi-move-all-results button.selected{border-color:var(--accent);background:color-mix(in srgb,var(--accent) 14%,transparent)}.mi-move-all-error{min-height:18px;color:#ffb7ad;font-size:12px}</style>';
const refineryCss = '<style>.mi-refinery-results{max-height:220px;overflow:auto;display:grid;gap:6px}.mi-refinery-results button{text-align:left;text-transform:none;font-weight:400;width:100%}.mi-refinery-results button.selected{border-color:var(--accent);background:color-mix(in srgb,var(--accent) 14%,transparent)}</style>';
const contributionCss = '<style>#mi-contribution-form .mi-quantity-field{align-self:start}#mi-contribution-form .mi-quantity-field input{height:44px;box-sizing:border-box;align-self:start}</style>';
const profileCss = '<style>.mi-profile-panel{width:min(620px,100%)}.mi-table .mi-profile-link{display:inline;padding:0;border:0;background:transparent;color:var(--accent);font:inherit;font-weight:700;text-transform:none;letter-spacing:normal;cursor:pointer;box-shadow:none}.mi-table .mi-profile-link:hover,.mi-table .mi-profile-link:focus-visible{color:var(--bright);text-decoration:underline;outline:0;background:transparent;box-shadow:none}.mi-profile-details{display:flex;gap:14px;align-items:flex-start;margin-top:22px}.mi-profile-avatar{width:88px;height:88px;object-fit:cover;border:1px solid var(--border)}.mi-profile-fields{display:grid;gap:8px;min-width:0}.mi-profile-fields div{display:grid;gap:3px}.mi-profile-fields span{color:var(--muted);font-size:11px;letter-spacing:.08em}.mi-profile-fields strong{color:var(--bright)}.mi-profile-rsi{margin-top:22px;color:var(--accent)}.mi-profile-rsi a{color:inherit}@media(max-width:520px){.mi-profile-details{flex-direction:column}}</style>';
const emptyMaterials = () => materials.map(material_name => ({ material_name, uex_code: codes[material_name] || 'ITEM', total_scu: 0, bands: {}, systems: {} }));
const bandFor = value => Object.entries(qualityRanges).find(([, range]) => value >= range[0] && value <= range[1])?.[0] || '';
const playerCell = row => row.profile_path ? `<button class="mi-profile-link" type="button" data-mi-profile-id="${esc(row.profile_path.split('/').pop())}">${esc(row.player_name)}</button>` : esc(row.player_name);

async function load() {
  try {
    state.error = '';
    const body = await request('/api/material-inventory/matrix?group_id=' + encodeURIComponent(state.groupId) + (state.mineOnly ? '&mine=1' : ''));
    const byCode = new Map((body.materials || []).map(material => [material.uex_code, material]));
    state.materials = emptyMaterials().map(material => byCode.get(material.uex_code) || material);
  } catch (error) { state.error = error.message; }
  render();
}

function render() {
  const el = root(); if (!el || location.hash !== '#material') return;
  const qualityBands = Object.entries(qualityRanges).map(([band, range]) => `<span>B${band}</span><span>${range[0]}–${range[1]}</span>`).join('');
  el.innerHTML = style + qualityInfoCss + `<section class="mi-app"><div class="eyebrow">MOBIGLASS GROUP MATERIAL TRACKER</div><h1>MATERIAL INVENTORY</h1><div class="mi-toolbar"><div class="mi-group-control"><label>GROUP<select id="mi-group">${state.groups.map(group => `<option value="${esc(group.id)}" ${group.id === state.groupId ? 'selected' : ''}>${esc(group.name)}</option>`).join('')}</select></label><button class="mi-quality-info" type="button" aria-label="Show quality band ranges" aria-describedby="mi-quality-tooltip">ⓘ</button><div class="mi-quality-tooltip" id="mi-quality-tooltip" role="tooltip"><strong>QUALITY BANDS</strong>${qualityBands}</div></div><button id="mi-refresh">REFRESH</button><button id="mi-add">＋ ADD CONTRIBUTION</button><button id="mi-move-all">MOVE ALL</button><button id="mi-mine" class="${state.mineOnly ? 'active' : ''}" aria-pressed="${state.mineOnly}">MY CONTRIBUTIONS</button></div>${state.error ? `<p class="mi-error">${esc(state.error)}</p>` : ''}<div class="mi-grid">${state.materials.map(material => `<button class="mi-card ${Number(material.total_scu) ? '' : 'empty'}" data-code="${esc(material.uex_code)}"><div class="mi-code">${esc(material.uex_code)}</div><div class="mi-name">${esc(material.material_name)}</div><div class="mi-scu">${Number(material.total_scu || 0).toFixed(1)} SCU</div><div class="mi-systems">${['Stanton','Pyro','Nyx'].map(system => `${Number(material.systems?.[system]?.total_scu || 0) > 0 ? '●' : '○'} ${system.slice(0,3).toUpperCase()}`).join(' ')}</div><div class="mi-meta">${Object.entries(material.bands || {}).map(([band, quantity]) => `B${band}: ${Number(quantity).toFixed(1)}`).join(' · ') || 'NO DATA'}</div></button>`).join('')}</div></section>`;
  el.querySelectorAll('.mi-meta').forEach(node => { if (node.textContent === 'NO DATA') node.textContent = 'NO STOCK'; });
  el.insertAdjacentHTML('afterbegin', logoCss);
  const heading = el.querySelector('.mi-app h1'), eyebrow = heading.previousElementSibling;
  heading.insertAdjacentHTML('beforebegin', '<div class="mi-brand"><img class="mi-logo" src="/assets/apps/materials.png" alt="Materials"><div class="mi-brand-copy"></div></div>');
  const brand = el.querySelector('.mi-brand'); brand.querySelector('.mi-brand-copy').append(eyebrow, heading);
  el.querySelector('#mi-group').onchange = event => { state.groupId = event.target.value; load(); };
  el.querySelector('#mi-refresh').onclick = load;
  el.querySelector('#mi-add').onclick = openContribution;
  el.querySelector('#mi-move-all').onclick = openMoveAllDialog;
  el.querySelector('#mi-mine').onclick = () => { state.mineOnly = !state.mineOnly; load(); };
  el.querySelectorAll('[data-code]').forEach(card => card.onclick = () => openDetail(card.dataset.code));
}

function addOverlay(content) { const overlay = document.createElement('div'); overlay.className = 'mi-overlay'; overlay.innerHTML = content; root().append(overlay); overlay.onclick = event => { if (event.target === overlay) overlay.remove(); }; return overlay; }

function openContribution(materialName = '') {
  const groupName = state.groups.find(group => group.id === state.groupId)?.name || 'SELECTED GROUP';
  const overlay = addOverlay(`${refineryCss}${contributionCss}<section class="mi-panel"><div class="mi-head"><div><div class="eyebrow">MATERIAL INVENTORY</div><h2>CONTRIBUTE MATERIAL</h2><div class="mi-group-target">TO GROUP · ${esc(groupName)}</div></div><button type="button" data-close>CLOSE</button></div><form class="mi-form" id="mi-contribution-form"><label>MATERIAL<select name="material_name">${materials.map(material => `<option>${esc(material)}</option>`).join('')}</select></label><label>QUALITY VALUE<input name="quality_value" type="number" min="1" max="1000" value="500" required></label><div class="wide" id="mi-band-display"></div><input name="quality_band" type="hidden" required><label class="mi-quantity-field">QUANTITY SCU<input name="quantity_scu" type="number" min="0.01" step="0.01" required></label><label>REFINERY<input name="refinery_query" type="search" autocomplete="off" placeholder="Search refinery stations" required><div class="mi-refinery-results" role="listbox"></div><input name="source_location" type="hidden" required></label><label class="wide">NOTE<textarea name="note" rows="3"></textarea></label><div class="mi-actions wide"><button type="button" data-close>CANCEL</button><button>SUBMIT</button></div></form></section>`);
  overlay.querySelectorAll('[data-close]').forEach(button => button.onclick = () => overlay.remove());
  const form = overlay.querySelector('#mi-contribution-form'), quality = form.elements.quality_value, band = form.elements.quality_band, display = overlay.querySelector('#mi-band-display');
  if (materialName && materials.includes(materialName)) form.elements.material_name.value = materialName;
  const refineryQuery=form.elements.refinery_query, refineryLocation=form.elements.source_location, refineryResults=overlay.querySelector('.mi-refinery-results');
  const searchRefineries=async()=>{try{const rows=(await request('/api/material-inventory/refineries?q='+encodeURIComponent(refineryQuery.value))).refineries||[];refineryResults.innerHTML=rows.map(row=>{const label=[row.name,row.planet_or_moon,row.star_system].filter(Boolean).join(' | ');return `<button type="button" data-refinery="${esc(label)}">${esc(label)}</button>`}).join('')||'<span>No refinery stations found</span>';refineryResults.querySelectorAll('[data-refinery]').forEach(button=>button.onclick=()=>{refineryLocation.value=button.dataset.refinery;refineryQuery.value=button.dataset.refinery;refineryResults.querySelectorAll('button').forEach(item=>item.classList.remove('selected'));button.classList.add('selected');});}catch(error){refineryResults.innerHTML=`<span class="mi-error">${esc(error.message)}</span>`;}};
  refineryQuery.oninput=searchRefineries; searchRefineries();
  const updateBand = () => { const value = Number(quality.value), result = bandFor(value); band.value = result; display.textContent = result ? `QUALITY BAND: B${result} · Quality ${qualityRanges[result][0]}–${qualityRanges[result][1]}` : 'QUALITY BAND: invalid quality'; };
  quality.oninput = updateBand; updateBand();
  form.onsubmit = async event => { event.preventDefault(); try { await request('/api/material-inventory/contributions', { method: 'POST', body: new URLSearchParams({ ...Object.fromEntries(new FormData(form)), group_id: state.groupId }) }); overlay.remove(); load(); } catch (error) { alert(error.message); } };
}

const materialProfileField = (label, value, fallback = '') => value ? `<div><span>${esc(label)}</span><strong>${esc(value)}</strong></div>` : fallback ? `<div><span>${esc(label)}</span><strong>${esc(fallback)}</strong></div>` : '';
async function openMaterialProfile(id) {
  const overlay = addOverlay(`${profileCss}<section class="mi-panel mi-profile-panel"><div class="mi-head"><div><div class="eyebrow">VERSELINK PUBLIC PROFILE</div><h2>LOADING PROFILE...</h2></div><button type="button" data-close>CLOSE</button></div></section>`);
  const close = () => overlay.remove(); overlay.querySelector('[data-close]').onclick = close;
  try {
    const body = await request('/api/public-profiles/' + encodeURIComponent(id)), profile = body.profile;
    overlay.innerHTML = `${profileCss}<section class="mi-panel mi-profile-panel"><div class="mi-head"><div><div class="eyebrow">VERSELINK PUBLIC PROFILE</div><h2>${esc(profile.display_name)}</h2></div><button type="button" data-close>CLOSE</button></div><div class="mi-profile-details">${profile.rsi_avatar_url ? `<img class="mi-profile-avatar" src="${esc(profile.rsi_avatar_url)}" alt="RSI avatar" referrerpolicy="no-referrer">` : ''}<div class="mi-profile-fields">${materialProfileField('RSI HANDLE', profile.rsi_handle)}${materialProfileField('CITIZEN RECORD', profile.rsi_citizen_record)}${materialProfileField('MAIN ORGANIZATION', profile.rsi_organization)}${materialProfileField('ENLISTED', profile.rsi_enlisted)}${materialProfileField('FLUENCY', profile.rsi_fluency)}${materialProfileField('DISCORD', profile.discord_name, 'Not provided')}</div></div>${profile.rsi_profile_url ? `<p class="mi-profile-rsi"><a href="${esc(profile.rsi_profile_url)}" target="_blank" rel="noopener noreferrer">OPEN RSI PROFILE ↗</a></p>` : ''}</section>`;
  } catch (error) {
    overlay.innerHTML = `${profileCss}<section class="mi-panel mi-profile-panel"><div class="mi-head"><div><div class="eyebrow">VERSELINK PUBLIC PROFILE</div><h2>PROFILE UNAVAILABLE</h2></div><button type="button" data-close>CLOSE</button></div><p class="mi-error">${esc(error.message)}</p></section>`;
  }
  overlay.querySelector('[data-close]').onclick = close;
}

async function openDetail(code) {
  try {
    const body = await request('/api/material-inventory/matrix/material/' + encodeURIComponent(code) + '?group_id=' + encodeURIComponent(state.groupId));
    const rows = (body.rows || body.contributions || []).filter(row => Number(row.available_scu ?? row.quantity_scu) > 0);
    const material = rows[0]?.material_name || state.materials.find(item => item.uex_code === code)?.material_name || code;
    const total = rows.reduce((sum, row) => sum + Number((row.available_scu ?? row.quantity_scu) || 0), 0);
    const bands = [...new Set(rows.map(row => `B${row.quality_band}`))].join(' · ') || 'NO STOCK', ownsRow = row => row.user_id === body.viewer_user_id;
    const overlay = addOverlay(`${profileCss}<section class="mi-panel"><div class="mi-head"><div><div class="eyebrow">MATERIAL INVENTORY</div><h2>${esc(material)}</h2></div><button type="button" data-close>CLOSE</button></div><div class="mi-summary"><div><span class="eyebrow">TRACKED</span><strong>${total.toFixed(1)} SCU</strong></div><div><span class="eyebrow">QUALITY BANDS</span><strong>${esc(bands)}</strong></div><div><span class="eyebrow">CONTRIBUTIONS</span><strong>${rows.length}</strong></div></div><table class="mi-table"><thead><tr><th>PLAYER</th><th>WAREHOUSE</th><th>QUALITY</th><th>BAND</th><th>SCU</th><th>NOTE</th><th>WITHDRAW</th><th>MOVE</th></tr></thead><tbody>${rows.map((row, index) => `<tr data-index="${index}"><td>${playerCell(row)}</td><td>${esc(row.source_location || 'Unknown')}</td><td>${row.quality_value}</td><td>B${row.quality_band}</td><td>${Number(row.available_scu ?? row.quantity_scu).toFixed(1)}</td><td>${row.notes?.length ? `<span class="mi-note" title="${esc(row.notes.join('\n'))}">ⓘ</span>` : ''}</td><td>${ownsRow(row) ? `<button data-withdraw="${index}">WITHDRAW</button>` : '<span class="mi-owner-only">OWNER ONLY</span>'}</td><td>${ownsRow(row) ? `<button data-move="${index}">MOVE</button>` : '<span class="mi-owner-only">OWNER ONLY</span>'}</td></tr>`).join('') || '<tr><td colspan="7">NO AVAILABLE CONTRIBUTIONS</td></tr>'}</tbody></table></section>`);
    const headActions = document.createElement('div'); headActions.className = 'mi-head-actions'; headActions.style.cssText = 'display:flex;gap:10px;flex-wrap:wrap;justify-content:flex-end';
    headActions.innerHTML = '<button type="button" data-contribute>＋ CONTRIBUTE</button>';
    const closeButton = overlay.querySelector('[data-close]'); closeButton.replaceWith(headActions); headActions.append(closeButton);
    headActions.querySelector('[data-contribute]').onclick = () => { overlay.remove(); openContribution(material); };
    closeButton.onclick = () => overlay.remove();
    overlay.querySelectorAll('[data-mi-profile-id]').forEach(button => button.onclick = () => openMaterialProfile(button.dataset.miProfileId));
    overlay.querySelectorAll('[data-withdraw]').forEach(button => button.onclick = () => openWithdrawDialog(rows[Number(button.dataset.withdraw)], material, code, overlay));
    overlay.querySelectorAll('[data-move]').forEach(button => button.onclick = () => openMoveDialog(rows[Number(button.dataset.move)], material, code, overlay));
  } catch (error) { alert(error.message); }
}

async function openMoveAllDialog() {
  const overlay = addOverlay(`${moveAllCss}<section class="mi-panel mi-move-all-panel"><div class="mi-head"><div><div class="eyebrow">MATERIAL INVENTORY</div><h2>MOVE ALL MATERIAL</h2><div class="mi-group-target">YOUR MATERIAL · ${esc(state.groups.find(group => group.id === state.groupId)?.name || 'SELECTED GROUP')}</div></div><button type="button" data-close>CLOSE</button></div><form class="mi-move-all-form"><p class="mi-move-all-info">All of your available material in this group will be moved to the selected refinery. Other players' material remains unchanged.</p><label>NEW LOCATION · SEARCH<input name="location_query" type="search" autocomplete="off" placeholder="Search Star Citizen locations" required></label><div class="mi-move-all-results" role="listbox"></div><input name="destination" type="hidden" required><div class="mi-move-all-error" role="alert"></div><div class="mi-actions"><button type="button" data-cancel>CANCEL</button><button type="submit">MOVE ALL MATERIAL</button></div></form></section>`);
  const close=()=>overlay.remove(); overlay.querySelector('[data-close]').onclick=close; overlay.querySelector('[data-cancel]').onclick=close;
  const form=overlay.querySelector('form'), query=form.elements.location_query, destination=form.elements.destination, results=overlay.querySelector('.mi-move-all-results'), error=overlay.querySelector('.mi-move-all-error'); let locations=[];
  const search=async()=>{try{locations=(await request('/api/material-inventory/locations?group_id='+encodeURIComponent(state.groupId)+'&q='+encodeURIComponent(query.value))).locations||[];results.innerHTML=locations.map(location=>{const label=[location.name,location.planet_or_moon,location.star_system].filter(Boolean).join(' | ');return `<button type="button" data-location="${esc(label)}">${esc(label)}</button>`}).join('')||'<span>No locations found</span>';results.querySelectorAll('[data-location]').forEach(button=>button.onclick=()=>{destination.value=button.dataset.location;query.value=button.dataset.location;results.querySelectorAll('button').forEach(item=>item.classList.remove('selected'));button.classList.add('selected');});}catch(exception){error.textContent=exception.message;}};
  query.oninput=search; await search();
  form.onsubmit=async event=>{event.preventDefault();if(!destination.value){error.textContent='Select a destination.';return;}try{await request('/api/material-inventory/move-all',{method:'POST',body:new URLSearchParams({group_id:state.groupId,destination:destination.value})});close();await load();}catch(exception){error.textContent=exception.message;}};
}

async function openMoveDialog(row, material, code, detailOverlay) {
  const available = Number(row.available_scu ?? row.quantity_scu), overlay = addOverlay(`${moveCss}<section class="mi-panel mi-move-panel"><div class="mi-head"><div><div class="eyebrow">MATERIAL INVENTORY</div><h2>MOVE MATERIAL</h2><div class="mi-group-target">WITHIN GROUP · ${esc(state.groups.find(group => group.id === state.groupId)?.name || 'SELECTED GROUP')}</div></div><button type="button" data-close>CLOSE</button></div><div class="mi-withdraw-details"><div><span>MATERIAL</span><strong>${esc(material)}</strong></div><div><span>AVAILABLE</span><strong>${available.toFixed(1)} SCU</strong></div><div><span>FROM</span><strong>${esc(row.source_location || 'Unknown')}</strong></div><div><span>QUALITY</span><strong>${row.quality_value} · B${row.quality_band}</strong></div></div><form class="mi-move-form"><label>QUANTITY SCU<input name="quantity_scu" type="number" min="0.01" max="${available}" step="0.01" required autofocus></label><label>NEW LOCATION · SEARCH<input name="location_query" type="search" autocomplete="off" placeholder="Search Star Citizen locations" required></label><div class="mi-location-results" role="listbox"></div><input name="destination" type="hidden" required><div class="mi-move-error" role="alert"></div><div class="mi-actions"><button type="button" data-cancel>CANCEL</button><button type="submit">MOVE STOCK</button></div></form></section>`);
  const close=()=>overlay.remove(); overlay.querySelector('[data-close]').onclick=close; overlay.querySelector('[data-cancel]').onclick=close;
  const form=overlay.querySelector('form'), query=form.elements.location_query, destination=form.elements.destination, results=overlay.querySelector('.mi-location-results'), error=overlay.querySelector('.mi-move-error'); let locations=[];
  const search=async()=>{try{locations=(await request('/api/material-inventory/locations?group_id='+encodeURIComponent(state.groupId)+'&q='+encodeURIComponent(query.value))).locations||[];results.innerHTML=locations.map(location=>{const label=[location.name,location.planet_or_moon,location.star_system].filter(Boolean).join(' | ');return `<button type="button" data-location="${esc(label)}">${esc(label)}</button>`}).join('')||'<span>No locations found</span>';results.querySelectorAll('[data-location]').forEach(button=>button.onclick=()=>{destination.value=button.dataset.location;query.value=button.dataset.location;results.querySelectorAll('button').forEach(item=>item.classList.remove('selected'));button.classList.add('selected');});}catch(exception){error.textContent=exception.message;}};
  query.oninput=search; await search(); form.onsubmit=async event=>{event.preventDefault();const quantity=Number(form.elements.quantity_scu.value);if(!Number.isFinite(quantity)||quantity<=0||quantity>available||!destination.value){error.textContent='Enter a valid quantity and select a destination.';return;}try{await request('/api/material-inventory/moves',{method:'POST',body:new URLSearchParams({group_id:state.groupId,material_name:row.material_name,quality_band:row.quality_band,quality_value:row.quality_value,contributor_user_id:row.user_id,source_location:row.source_location||'',destination:destination.value,quantity_scu:quantity})});close();detailOverlay.remove();await load();await openDetail(code);}catch(exception){error.textContent=exception.message;}};
}

function openWithdrawDialog(row, material, code, detailOverlay) {
  const available = Number(row.available_scu ?? row.quantity_scu);
  const overlay = addOverlay(`${withdrawCss}<section class="mi-panel mi-withdraw-panel"><div class="mi-head"><div><div class="eyebrow">MATERIAL INVENTORY</div><h2>WITHDRAW MATERIAL</h2><div class="mi-group-target">FROM GROUP · ${esc(state.groups.find(group => group.id === state.groupId)?.name || 'SELECTED GROUP')}</div></div><button type="button" data-close>CLOSE</button></div><div class="mi-withdraw-details"><div><span>MATERIAL</span><strong>${esc(material)}</strong></div><div><span>AVAILABLE</span><strong>${available.toFixed(1)} SCU</strong></div><div><span>WAREHOUSE</span><strong>${esc(row.source_location || 'Unknown')}</strong></div><div><span>QUALITY</span><strong>${row.quality_value} · B${row.quality_band}</strong></div></div><form class="mi-withdraw-form"><label>WITHDRAW QUANTITY SCU<input name="quantity_scu" type="number" min="0.01" max="${available}" step="0.01" required autofocus></label><div class="mi-withdraw-error" role="alert"></div><div class="mi-actions"><button type="button" data-cancel>CANCEL</button><button type="submit">WITHDRAW</button></div></form></section>`);
  const close = () => overlay.remove();
  overlay.querySelector('[data-close]').onclick = close; overlay.querySelector('[data-cancel]').onclick = close;
  const form = overlay.querySelector('form'), input = form.elements.quantity_scu, error = overlay.querySelector('.mi-withdraw-error'); input.focus();
  form.onsubmit = async event => {
    event.preventDefault(); const quantity = Number(input.value);
    if (!Number.isFinite(quantity) || quantity <= 0 || quantity > available) { error.textContent = `Enter a quantity between 0.01 and ${available.toFixed(1)} SCU.`; return; }
    try {
      await request('/api/material-inventory/withdrawals', { method: 'POST', body: new URLSearchParams({ group_id: state.groupId, material_name: row.material_name, quality_band: row.quality_band, quality_value: row.quality_value, contributor_user_id: row.user_id, warehouse: row.source_location || '', quantity_scu: quantity }) });
      overlay.remove(); detailOverlay.remove(); await load(); await openDetail(code);
    } catch (exception) { error.textContent = exception.message; }
  };
}

export async function mount() {
  if (location.hash !== '#material') return;
  try { state.groups = (await request('/api/groups')).groups || []; state.groupId = state.groupId || state.groups[0]?.id || ''; await load(); } catch (error) { state.error = error.message; render(); }
}
